language: rust
sudo: required
dist: trusty
rust: nightly

matrix:
  include:
    - os: linux
      env: TARGET=x86_64-unknown-linux-gnu
      script: cargo test --verbose
    - os: osx
      env: TARGET=x86_64-apple-darwin
      script: cargo test --verbose
